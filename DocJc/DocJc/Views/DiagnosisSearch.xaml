<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:DocJc.Converters;assembly=DocJc"
             xmlns:viewModels="clr-namespace:DocJc.ViewModels;assembly=DocJc"
             x:Name="DiagnosisSearchView"
             x:Class="DocJc.Views.DiagnosisSearch"
             Title="{Binding Title}"
             BindingContext="{Binding DiagnosisSearchViewModel, 
                Source={StaticResource Locator}}" >
    <Page.Resources>
        <converters:PositiveIntegerToBooleanConverter x:Key="PositiveIntegerToBooleanConverter"/>
        <converters:SymptomListToStringConverter x:Key="SymptomListToStringConverter"/>
    </Page.Resources>

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Editor Placeholder="Search for symptoms..." 
                Text="{Binding Path=SymptomSearchCriteria}"
                Grid.Row="0"/>
        <StackLayout
            Grid.Row="1"
            Padding="3"
            BackgroundColor="DarkSeaGreen" 
            Orientation="Horizontal"
            Margin="0"
            BindableLayout.ItemsSource="{Binding SelectedSymptoms}">
            <BindableLayout.ItemTemplate>
                <DataTemplate>
                    <Label 
                        Text="{Binding Name}"
                        FontSize="Medium"></Label>
                </DataTemplate>
            </BindableLayout.ItemTemplate>
        </StackLayout>
        <!--<Image Grid.Row="2"
               IsVisible="{Binding IsLoading}"
               Source="https://media.giphy.com/media/xTk9ZvMnbIiIew7IpW/giphy.gif" />-->
        <Frame Grid.Row="2"
               IsVisible="{Binding FoundDiagnostics.Count, 
                    Converter={StaticResource PositiveIntegerToBooleanConverter}}"
               BackgroundColor="GreenYellow" 
               Padding="0"
               Margin="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!--<StackLayout Grid.Row="0">
                    <Label 
                        Text="{Binding SelectedSymptoms, 
                        Converter={StaticResource ListToStringConverter}}"></Label>
                </StackLayout>-->
                <StackLayout Grid.Column="0"
                             Orientation="Horizontal"
                             Padding="5">
                    <Label FontSize="Large" Text="Diagnostics found:"></Label>
                    <Label FontSize="Large" Text="{Binding FoundDiagnostics.Count}"></Label>
                </StackLayout>
                <StackLayout Grid.Column="2"
                             Orientation="Horizontal"
                             Padding="5">
                    <Label
                        TextColor="Blue"
                        FontAttributes="Bold"
                        FontSize="Large" 
                        Text="See details"></Label>
                </StackLayout>
            </Grid>
        </Frame>

        <ListView Grid.Row="3"
                  ItemsSource="{Binding FoundSymptoms}"
                  IsVisible="{Binding AreSymptomsReady}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout 
                            Orientation="Horizontal"
                            Padding="10" >
                            <StackLayout.GestureRecognizers>
                                <TapGestureRecognizer 
                                    Command="{Binding SymptomTappedCommand, 
                                        Source={RelativeSource AncestorType={x:Type viewModels:DiagnosisSearchViewModel}}}"
                                    CommandParameter="{Binding .}"/>
                            </StackLayout.GestureRecognizers>

                            <Image IsVisible="{Binding IsSelected}"
                                   Source="tick.png" 
                                   HeightRequest="20" 
                                   WidthRequest="20" />

                            <Label Text="{Binding Name}"
                                   LineBreakMode="NoWrap" 
                                   Style="{DynamicResource ListItemTextStyle}"
                                   FontSize="16" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!--IsVisible="{Binding FoundDiagnostics, Converter={StaticResource ListAnyToBooleanConverter}}">-->

        <!--<ListView Grid.Row="4"
                  ItemsSource="{Binding FoundDiagnostics}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Padding="10" >
                            <Label Text="{Binding Issue.Name}"
                                   LineBreakMode="NoWrap" 
                                   Style="{DynamicResource ListItemTextStyle}"
                                   FontSize="16" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->
    </Grid>
</ContentPage>
